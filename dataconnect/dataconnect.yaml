specVersion: "v1"
serviceId: "inventory-management"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "inventory-management-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
